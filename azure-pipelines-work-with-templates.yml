# File: azure-pipelines.yml
trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Install NodeJS
    jobs:
      - job: nodejsinstall
        steps:
          - task: NodeTool@0
            inputs:
              versionSource: '16.14.2'
              checkLatest: true

  - stage: Install npm
    jobs:
      - job: npminstall
        steps:
          - task: Npm@1
            inputs:
              command: 'install'
  - template: templates/azure-templates-stages.yml