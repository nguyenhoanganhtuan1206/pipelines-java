stages:
  - stage: Angular
    jobs:
      - job: angularInstall
        steps:
          - script: npm install angular
  - stage: Build
    jobs:
      - job: npmRunBuild
        steps:
          - script: npm run build