jobs:
  - job: ubuntu-latest
    pool:
      vmImage: 'ubuntu-latest'
    steps:
      - bash: echo "Hello World!"
  - job: windows-latest
    pool:
      vmImage: 'windows-latest'
    steps:
      - bash: echo "Hello World!"