trigger:
  - main

pool:
  vmImage: 'ubuntu-latest'

stages:
  - stage: Install
    jobs:
      - job: npmInstall
        steps:
        - task: Npm@1
          inputs:
            command: 'install'
  - template: templates/azure-templates-stages.yml